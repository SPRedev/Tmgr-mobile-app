import 'package:flutter/material.dart';

class NavigationService {
  // Create a static, global key for the Navigator.
  static final GlobalKey<NavigatorState> navigatorKey =
      GlobalKey<NavigatorState>();
}
